import AttendanceTable from "@/features/attendance/components/AttendanceTable";

export const metadata = {
  title: "الحضور والغياب | نظام المدارس",
};

export default function AttendancePage() {
  return <AttendanceTable />;
}
