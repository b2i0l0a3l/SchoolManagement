import StudentTable from "@/features/students/components/StudentTable";

export const metadata = {
  title: "إدارة الطلاب | نظام المدارس",
};

export default function StudentsPage() {
  return <StudentTable />;
}
